import React from 'react';
import {Container} from '../globalStyles';

const Register = () => {
    return (
        <Container>
            REGISTER
        </Container>
    );
};

export default Register
